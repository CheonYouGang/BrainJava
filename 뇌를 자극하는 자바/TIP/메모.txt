아스키코드(ASC) --> 한글x
유니코드(UTF-8) --> 한글o

함수(메소드) 구조

1. 함수
2. 기능
3. 함수(괄호) 안에 들어갈 것들: 인자 or 인수 or 매개변수 or Arguement, 반환(return), void
4. (시작블럭->){	}(<-끝블럭) <-- 블레이스



라이브러리 함수(미리 제공된 함수)
	[이곳에 반환값, 타입 적어준다(변수, 타입 등)] Upper(문자){
		;(<- 문장의 끝)
        }